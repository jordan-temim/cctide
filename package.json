{
  "name": "cctide",
  "private": true,
  "version": "0.2.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "build:mac": "npm run tauri build -- --target universal-apple-darwin && mkdir -p build && mv src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg build/",
    "build:win": "npm run tauri build && md build & move src-tauri\\target\\release\\bundle\\msi\\*.msi build\\"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.4.3",
    "typescript": "~5.9.3"
  }
}
