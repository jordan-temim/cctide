{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | { read -r f; case \"$f\" in *.rs) cargo fmt --manifest-path src-tauri/Cargo.toml ;; esac; } 2>/dev/null || true",
            "statusMessage": "rustfmt"
          }
        ]
      }
    ]
  }
}
